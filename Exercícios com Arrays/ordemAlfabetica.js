const ordemAlfabetica = (arrayComStrings) => {
    const arrayOrganizada = arrayComStrings.sort()
    return arrayOrganizada
} 
console.log(ordemAlfabetica([])) // Digite um Array de strings dentro da Função